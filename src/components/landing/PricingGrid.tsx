'use client'
import { useState } from 'react'
import Link from 'next/link'
type Tab='creation'|'subscription'
const PLANS=[['가볍게 시작','99,000원','소개, 사진, 지도','처음 만드는 분께'],['가장 많이 선택','199,000원','예약, 문의, 알림','작은 가게와 모임에'],['제대로 시작','299,000원','로그인, 결제, 통계','서비스를 키우는 분께']]
export default function PricingGrid(){const[tab,setTab]=useState<Tab>('creation');return <section id="pricing" className="section-paper border-t border-border"><div className="mx-auto max-w-6xl px-5 py-20 sm:px-8"><div className="flex flex-col justify-between gap-6 md:flex-row md:items-end"><div><p className="eyebrow">가격 안내</p><h2 className="section-title mt-5">복잡한 계산 없이,<br /><span className="text-primary">필요한 만큼만.</span></h2></div><div className="tab-switch"><button onClick={()=>setTab('creation')} className={tab==='creation'?'is-active':''}>한 번 결제</button><button onClick={()=>setTab('subscription')} className={tab==='subscription'?'is-active':''}>매달 결제</button></div></div>{tab==='creation'?<div className="mt-12 grid gap-3 md:grid-cols-3">{PLANS.map(([name,price,features,forWho],i)=><article key={name} className={`price-card ${i===1?'is-featured':''}`}><p className="eyebrow">{i===1?'추천':`0${i+1}`}</p><h3>{name}</h3><strong>{price}</strong><p className="price-feature">{features}</p><small>{forWho}</small><Link href="/builder">이 구성으로 시작 →</Link></article>)}</div>:<div className="subscription-card mt-12"><p className="eyebrow">매달 결제</p><h3>서버와 페이지를<br /><span>안심하고 유지하세요.</span></h3><strong>월 9,900원부터</strong><Link href="/builder" className="action-button">내 페이지 만들기 →</Link></div>}<p className="mt-8 text-xs text-muted-foreground">모든 플랜은 직접 수정할 수 있습니다. 먼저 둘러보고 결정하세요.</p></div></section>}
